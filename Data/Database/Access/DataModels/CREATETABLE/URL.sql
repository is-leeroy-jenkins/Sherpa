CREATE TABLE URL 
(
    UrlsId AUTOINCREMENT NOT NULL UNIQUE,
    Name TEXT(80) NULL DEFAULT NS,
    Location TEXT(80) NULL DEFAULT NS,
    Address TEXT(80) NULL DEFAULT NS,
    CONSTRAINT UrlPrimaryKey
        PRIMARY KEY(SourceId)
);