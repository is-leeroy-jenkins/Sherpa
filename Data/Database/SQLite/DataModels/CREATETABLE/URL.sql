CREATE TABLE IF NOT EXISTS URL 
(
	UrlId	INTEGER NOT NULL UNIQUE,
	Name	TEXT(80) NULL DEFAULT NS,
	Location	TEXT(80) NULL DEFAULT NS,
	Address	TEXT(80) NULL DEFAULT NS,
	PRIMARY KEY(SourceId AUTOINCREMENT)
);